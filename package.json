{
  "name": "backend",
  "version": "1.0.0",
  "description": "Node, Express, MongoDB and JWT | Create a web application using server side JavaScript. On server side, Node.js and the famous Express.js framework. We have protected our resources with JSON Web Token (JWT). It also allow to perform Authentication and Authorization.",
  "main": "index.js",
  "scripts": {
    "start": "nodemon server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["Node.js", "Express", "MongoDB","JWT", "JSON Web Token"],
  "author": "Rufin Hounkpe",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1"
  },
  "devDependencies": {
    "nodemon": "^1.19.4"
  }
}
