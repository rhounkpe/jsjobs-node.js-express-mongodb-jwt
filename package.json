{
  "name": "backend",
  "version": "1.0.0",
  "description": "Angular, Node, Express and JWT |  Create a web application using client and server side JavaScript. On the client side, we used Angular 4 with upgrade to Angular 8. On server side, Node.js and the famous Express.js framework. We have protected our resources with JSON Web Token (JWT). It also allow to perform Authentication and Authorization, on both Server and Client Side.",
  "main": "index.js",
  "scripts": {
    "start": "nodemon server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Rufin Hounkpe",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "jwt-decode": "^2.2.0"
  },
  "devDependencies": {
    "nodemon": "^1.19.4"
  }
}
